#!/usr/bin/env python
# encoding: utf-8


def validate(lst):
    for item in lst:
        if hasattr(item, 'thrift_spec'):
            item.validate()
            validate(item.__dict__.itervalues())
        elif type(item) == list:
            validate(item)
        else:
            pass
